# Documentation rules (detail)

- `README.md` is user-facing: install, node reference, examples.
- Ship runnable example flows under `examples/*.json`; reference them from the README.
- Keep architecture docs out of the README, under `doc/architecture/`.
- Keep `AGENTS.md` self-contained so each repo is portable to any AI tool on its own.
